type Query {
    hello: String!
}

type Mutation {
    hello(msg: String!): String!
}

interface Identifiable {
  id: ID!
}

type OperationFailed {
    reason: String!
}

schema {
    query: Query
    mutation: Mutation
}